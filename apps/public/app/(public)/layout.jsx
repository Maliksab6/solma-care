export const metadata = {
  title: {
    template: '%s | Solma Care',
    default: 'Solma Care — Hormonal Health for South Asian Women',
  },
}

export default function PublicLayout({ children }) {
  return children
}
